console.log(document.querySelector('.modal-body'));

// .innerHTML("<%= j render(partial: '_form.html.erb', locals: {@company => @company, @categories = Category.all.map { |category| [category.name, category.id]} } ) %>");
